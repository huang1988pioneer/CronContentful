name: Cron Contentful

on:
  schedule:
    # GitHub cron uses UTC. 21:05 UTC is 05:05 in Asia/Taipei.
    - cron: "5 21 * * *"
    # Odd hours at :33 in Asia/Taipei, starting from 05:33 through 23:33.
    - cron: "33 1,3,5,7,9,11,13,15,21,23 * * *"
    # Even hours at :33 in Asia/Taipei, starting from 06:33 through 22:33.
    - cron: "33 0,2,4,6,8,10,12,14,22 * * *"
  workflow_dispatch:
    inputs:
      action:
        description: "auto, start, add, or delete"
        required: false
        default: "add"

jobs:
  cron-contentful:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Run CronContentful task
        run: npm run cron
        env:
          CONTENTFUL_SPACE_ID: ${{ secrets.CONTENTFUL_SPACE_ID }}
          CONTENTFUL_ENVIRONMENT_ID: ${{ secrets.CONTENTFUL_ENVIRONMENT_ID || 'master' }}
          CONTENTFUL_MANAGEMENT_TOKEN: ${{ secrets.CONTENTFUL_MANAGEMENT_TOKEN }}
          CONTENTFUL_LOCALE: ${{ secrets.CONTENTFUL_LOCALE || 'en-US' }}
          CRON_TIME_ZONE: Asia/Taipei
          CRON_ACTION: ${{ inputs.action || 'auto' }}
